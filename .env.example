# Stripe Configuration
STRIPE_SECRET_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=

# Firebase Configuration (Already in firebase-applet-config.json, but these can be used as overrides)
# GEMINI_API_KEY is handled by the platform
GEMINI_API_KEY=
